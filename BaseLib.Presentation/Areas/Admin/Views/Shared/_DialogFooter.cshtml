<div class="flex items-center justify-end gap-2 border-t border-navy-100 px-6 py-4">
    <a href="@(ViewData["CancelUrl"] ?? Url.Action("Index"))" class="btn-soft">İptal</a>
    <button type="submit" class="btn" data-saving-text="Kaydediliyor…">Kaydet</button>
</div>
