<div class="flex items-center justify-between border-b border-navy-100 px-6 py-4">
    <h2 id="edit-title" class="text-lg font-bold text-navy-900">@ViewData["Title"]</h2>
    <a href="@(ViewData["CancelUrl"] ?? Url.Action("Index"))" data-dialog-cancel aria-label="Kapat" class="icon-button">
    <partial name="_Icon" model='@("close")' />
    </a>
</div>
